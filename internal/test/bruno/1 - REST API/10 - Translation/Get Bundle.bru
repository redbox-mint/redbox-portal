meta {
  name: Get Bundle
  type: http
  seq: 3
}

get {
  url: {{host}}/default/rdmp/api/i18n/bundles/en/translation
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("Status code is 200", function () {
      expect([200]).to.include(res.getStatus());
  });
}
