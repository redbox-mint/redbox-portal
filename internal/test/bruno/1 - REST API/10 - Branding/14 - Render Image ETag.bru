meta {
  name: Render Image with ETag (200)
  type: http
  seq: 14
}

get {
  url: {{host}}/default/rdmp/images/logo
  auth: none
}

headers {
  If-None-Match: W/"<not-a-valid-tag>"
}

tests {
  test("Status code is 200 when ETag doesn't match", function () {
    const status = res.getStatus();
    expect(status).to.equal(200);
  });
}