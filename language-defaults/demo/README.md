# Demo language defaults

These locale files are examples and development fixtures. They are not production
locale defaults and are not loaded by the portal's bootstrap or translation sync
commands. Keep production locales as direct children of `language-defaults/`.

The demo set currently includes Spanish (`es`), Italian (`it`), Māori (`mri`), and
Simplified Chinese (`zh-cn`).
