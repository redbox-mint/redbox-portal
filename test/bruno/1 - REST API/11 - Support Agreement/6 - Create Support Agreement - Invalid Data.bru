meta {
  name: Create Support Agreement - Invalid Data
  type: http
  seq: 6
}

post {
  url: {{host}}/default/rdmp/api/support-agreements/2026
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "releaseNotes": []
  }
}

tests {
  test("Status code is 400 Bad Request", function () {
    expect(res.getStatus()).to.equal(400);
  });

  test("Response indicates agreedSupportDays is required", function () {
    var jsonData = res.getBody();
    expect(jsonData).to.have.property('message');
    expect(jsonData.message).to.include('agreedSupportDays');
  });
}
