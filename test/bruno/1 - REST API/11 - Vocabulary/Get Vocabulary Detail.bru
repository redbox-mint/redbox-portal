meta {
  name: Get Vocabulary Detail
  type: http
  seq: 3
}

get {
  url: {{host}}/default/rdmp/api/vocabulary/{{vocabularyId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("Status code is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
