meta {
  name: Delete Entry
  type: http
  seq: 10
}

delete {
  url: {{host}}/default/rdmp/api/i18n/entries/ar/translation/hello-world
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
}
