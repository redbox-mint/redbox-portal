meta {
  name: Preview second fetch 404
  type: http
  seq: 48
}

get {
  url: {{host}}{{preview_single_url}}
  body: none
  auth: none
}

headers {
     Authorization: Bearer {{token}}
}

tests {
  test("Second fetch 404 (single use)", function(){ expect(res.getStatus()).to.equal(404); });
}
