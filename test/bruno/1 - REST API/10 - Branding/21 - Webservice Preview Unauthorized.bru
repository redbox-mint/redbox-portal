meta {
  name: Webservice Preview Unauthorized
  type: http
  seq: 21
}

post {
  url: {{host}}/default/rdmp/api/branding/preview
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Status code is 401 (unauthenticated request rejected)", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
