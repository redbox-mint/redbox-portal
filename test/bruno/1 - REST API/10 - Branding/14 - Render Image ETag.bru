meta {
  name: Render Image with ETag (200)
  type: http
  seq: 14
}

get {
  url: {{host}}/default/rdmp/images/logo
  auth: none
}

headers {
  If-None-Match: W/"logo-test-hash"
}

tests {
  test("Status code is 200 (depending on ETag match)", function () {
    const status = res.getStatus();
    expect(status).to.equal(200);
  });
}