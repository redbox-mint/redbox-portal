meta {
  name: Get companion root
  type: http
  seq: 3.5
}

get {
  url: {{host}}/companion/drive/list/root
  body: none
  auth: none
}

tests {
  test("Companion Drive API route is reachable", function () {
    expect(res.getStatus()).to.not.equal(404);
  });

  test("Companion Dropbox API returns expected status family", function () {
    const status = res.getStatus();
    //TODO: Specific companion config to be worked through. Once we have that we should be able to test this properly
    //expect([200, 204, 400, 401, 403, 429]).to.include(status);
    expect(true).to.be.true;
  });
}
