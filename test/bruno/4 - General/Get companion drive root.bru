meta {
  name: Get companion drive root
  type: http
  seq: 3.6
}

get {
  url: {{host}}/companion/drive/list/root
  body: none
  auth: none
}

tests {
  test("Companion Drive API route is reachable", function () {
    expect(res.getStatus()).to.not.equal(404);
  });

  test("Companion Drive API returns expected status family", function () {
    const status = res.getStatus();
    expect([200, 204, 400, 401, 403, 429, 500]).to.include(status);
  });
}
