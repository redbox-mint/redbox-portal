meta {
  name: App Branding - Config Not Found (404)
  type: http
  seq: 39
}

get {
  url: {{host}}/nonexistent/rdmp/app/branding/config
  body: none
  auth: none
}

headers {
  X-Source: jsclient
  Accept: application/json, text/plain, */*
}

script:pre-request {
  const cookie = bru.getVar("cookie");
  if(cookie) { req.setHeader("Cookie", cookie); }
}

tests {
  test("Status code is 404", function(){
    expect(res.getStatus()).to.equal(404);
  });
  
}
