meta {
  name: App Branding - Draft Contrast Violation (400)
  type: http
  seq: 41
}

post {
  url: {{host}}/default/rdmp/app/branding/draft
  body: json
  auth: none
}

headers {
  X-Source: jsclient
  Content-Type: application/json
}

body:json {
  {
    "variables": {
      "site-branding-area-background-color": "#ffffff",
      "site-branding-area-text-color": "#ffff00"
    }
  }
}

script:pre-request {
  const bruHelper = require("./bruno-helpers.js");
  bruHelper.setCookies({bru, req});
}

tests {
  test("Status code is 400 for contrast violation ", function () {
    const status = res.getStatus();
    expect([400]).to.include(status);
  });
}
